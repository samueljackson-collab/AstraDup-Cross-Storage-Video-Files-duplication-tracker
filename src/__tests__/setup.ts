// NOTE: This app uses @google/genai 0.14.0 (legacy API surface), not the ^1.x
// version used by most other apps in the portfolio. When mocking Gemini calls in
// tests, import from '@google/genai' at that version's module shape.
//
// @testing-library/jest-dom is not yet installed in this project.
// Before running tests, install the required testing dependencies:
//
//   npm install -D @testing-library/jest-dom @testing-library/react @testing-library/user-event vitest @vitest/coverage-v8 jsdom
//
// Then uncomment the line below:
// import '@testing-library/jest-dom';
